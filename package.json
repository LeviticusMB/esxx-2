{
  "name": "esxx-2",
  "version": "1.0.0",
  "description": "The friendly Node-based Web Application Runtime, take 2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "tsc": "tsc",
    "build": "yarn run clean && yarn run tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Martin Blom <martin@blom.org>",
  "license": "Apache-2.0",
  "dependencies": {
    "@reactivex/rxjs": "^5.0.3",
    "@types/node": "^7.0.1",
    "@types/request": "~0.0.39",
    "request": "^2.79.0",
    "typescript": "^2.1.5",
    "uri-js": "^2.1.1"
  }
}
